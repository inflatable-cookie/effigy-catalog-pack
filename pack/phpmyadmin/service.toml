[service]
name = "phpmyadmin"
description = "phpMyAdmin web UI for local MariaDB and MySQL access"

[params.version]
type = "string"
default = "5.2.3"
description = "phpMyAdmin image tag (pinned; 5.2.3 is the digest-verified `latest` as of 2026-07)"

[params.database_host]
type = "string"
default = "db"
description = "Database host name"

[params.database_port]
type = "integer"
default = 3306
description = "Database port"

[params.database_password]
type = "string"
default = ""
description = "Database password for the root user"

[capabilities]
exec_target = false

[depends_on]
optional = ["mariadb", "postgres"]

[ports]
default = [80]
description = "HTTP port"
